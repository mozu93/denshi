# インデックス再構築機能の改善案：メモの保持

## 概要

現在の「インデックス再構築」機能は、ファイル名から情報を再生成するため、登録時にユーザーが入力した「メモ」の内容が消えてしまう。
この問題を解決するため、既存のインデックス情報を活用し、メモの内容を新しいインデックスに引き継ぐように機能を改善する。

## 実装方針（案1：既存インデックスの活用）

`models/metadata_manager.py` の `rebuild_index` メソッドを以下の手順で修正する。

1.  **古いインデックス情報の読み込みと整形**
    *   再構築を開始する前に、年ごとの既存の `index.csv` を `pandas` DataFrameとして読み込む。
    *   ファイルパスをキー、メモを値とする辞書（例: `memo_map = {'path/to/file1.pdf': 'memo1', ...}`）を作成しておく。
    *   この時、ファイルパスはOSに依存しない形式（例: `/` 区切り）に正規化しておくことが望ましい。

2.  **ファイルシステムのスキャン**
    *   従来通り、保存先ディレクトリを再帰的にスキャンし、PDFファイルの一覧を取得する。

3.  **新しいインデックス情報の生成とメモの引き継ぎ**
    *   スキャンした各PDFファイルについて、ファイル名から基本情報（日付、金額、取引先など）を抽出する。
    *   新しいインデックスレコードを生成する際に、手順1で作成した `memo_map` から、現在のファイルパスに一致するメモを検索する。
    *   メモが見つかった場合は、その内容を新しいレコードの `memo` フィールドにセットする。見つからなかった場合は空文字列をセットする。

4.  **新しいインデックスの保存**
    *   すべてのファイルの処理が終わったら、生成された新しいインデックス情報（メモ付き）で `index.csv` を上書き保存する。

## メリット

*   `index.csv` が破損していなければ、既存のメモをすべて保持できる。
*   PDFファイル自体への変更が不要なため、実装が比較的容易で、副作用のリスクが低い。

## 注意点

*   `index.csv` が読み込めないほど破損している場合や、ファイルが存在しない場合は、メモの復元はできない。その場合のフォールバック処理（ログ出力など）を考慮する。