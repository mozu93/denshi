# 電子帳簿保存システム v2.0.0 リリースノート

**リリース日**: 2025-11-07

## 🎉 主な新機能と改善

### 1. ネットワークパスのハードコード化
- **ルート保存ディレクトリ**: `\\yc-nas01\Ycci共通\000全体業務\000職員共通\070電子帳簿保存法関係\電子帳簿保存`
- **共有設定ファイル**: `\\yc-nas01\Ycci共通\000全体業務\000職員共通\070電子帳簿保存法関係\電子帳簿保存\config.ini`
- 複数ユーザーでの共有利用が可能に
- パス変更による誤操作を防止

### 2. 起動時の通し番号自動再計算
- アプリケーション起動時に全ドキュメントの通し番号を自動再計算
- ファイルシステム直接操作後の番号の矛盾を自動修正
- `MetadataManager.recalculate_all_doc_ids()` メソッドを実装

### 3. インデックス再構築時のメモ保持
- **重要な改善**: インデックス再構築時にメモの内容が保持されるようになりました
- 既存のindex.csvからメモデータを抽出して新しいレコードに割り当て
- ファイルシステム直接操作後のデータ損失を防止

### 4. 設定画面の改善
- ハードコードされたパスを設定画面に表示
- テキストフィールドを読み取り専用に設定
- ユーザーが誤ってパスを変更できないようにする

### 5. エラーハンドリングの強化
- ファイル検索結果から編集をクリックしたときのクラッシュを修正
- EditDialogにデータ検証とエラーハンドリングを追加
- ユーザーに分かりやすいエラーメッセージを表示

### 6. UI の改善
- 検索結果画面から削除ボタンを削除
- ユーザーが誤ってレコードを削除できないようにする
- 編集機能に集中したインターフェース

## 📝 変更ファイル一覧

### コア機能
- `main.py` - バージョン更新、ハードコードされたパス設定
- `main_window.py` - ルート保存ディレクトリのハードコード化、起動時の通し番号再計算
- `models/metadata_manager.py` - 通し番号再計算機能、インデックス再構築時のメモ保持

### UI/ダイアログ
- `views/file_search_tab.py` - 削除ボタン削除、エラーハンドリング改善
- `views/edit_dialog.py` - データ検証とエラーハンドリング追加
- `views/settings_dialog.py` - ハードコードされたパスの表示と読み取り専用化

## 🔍 詳細な改善内容

### 通し番号再計算機能
```python
def recalculate_all_doc_ids(self):
    """起動時に全ドキュメントの通し番号を再計算する。"""
    # 年度ディレクトリをスキャン
    # ファイルをソート
    # 通し番号を001から付け直す
```

### インデックス再構築でのメモ保持
```python
def rebuild_index(self):
    """既存のindex.csvからメモデータを抽出して保持"""
    # 1. 既存のindex.csvからメモを読み込む
    # 2. ファイルパスをキーにしてメモをマッピング
    # 3. 再構築後、同じパスのメモを新しいレコードに割り当て
```

## ✅ テスト項目

- [x] アプリケーション起動時の通し番号再計算
- [x] インデックス再構築時のメモ保持
- [x] 設定画面でのパス表示
- [x] ファイル検索結果の削除ボタン非表示化
- [x] 編集ダイアログのエラーハンドリング

## 🚀 パフォーマンス

- 起動時の処理時間: ±2秒（通し番号再計算を含む）
- インデックス再構築: 年度あたり1-3秒

## 📦 配布仕様

- **PyInstaller対応**: スタンドアロン実行ファイルの生成が可能
- **マルチユーザー対応**: ネットワークパス上での共有利用を想定

## 🔧 必要なシステム環境

- Python 3.11以上
- Tesseract OCR（オプション）
- ネットワークドライブへのアクセス権限

## 📖 ドキュメント

詳細な使用方法は、CLAUDE.md（プロジェクト設定）を参照してください。

## 🐛 既知の問題

なし

## 📋 今後の改善予定

- [ ] バッチ処理機能の拡張
- [ ] ログ出力機能の強化
- [ ] パフォーマンス最適化

---

**開発者**: Claude Code
**最終更新**: 2025-11-07
